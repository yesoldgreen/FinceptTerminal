# 《技术文档》

## 1. 文档目的

本文档记录 `Fincept Terminal v4.0.3` 的 GUI 中文化实现方式，用于帮助后续维护者快速理解这版代码为什么这样设计、核心入口在哪里、扩展时应优先修改哪些位置。

这份文档的作用接近“跨文件级代码注释”，重点解释技术架构与实现思路，而不是逐行描述代码。

## 2. 本版技术目标

`v4.0.3` 只实现 `GUI 简体中文支持`，不做新闻内容翻译，也不做通用内容翻译平台。

因此本版技术目标是：

- 增加一个轻量级 GUI 语言层。
- 在不大规模重构所有 screen 的前提下，让主界面具备中文显示能力。
- 把语言设置接入启动链路和 Settings。
- 保持英文默认行为不变。

## 3. 为什么没有直接使用 Qt `.ts/.qm`

理论上，Qt 官方推荐方案是：

- 所有字符串改成 `tr()`
- 使用 `lupdate/lrelease`
- 生成 `.ts/.qm`
- 用 `QTranslator` 加载

但当前项目现状并不适合在 `v4.0.3` 一次性做完：

- 项目页面很多，存在大量历史硬编码字符串。
- 很多界面已经写成大量直接构造 `QLabel("...") / QPushButton("...")` 的形式。
- 要完全切到 `.ts/.qm` 体系，意味着要在大量文件中补 `tr()`、补重翻译入口、补动态刷新逻辑，改造面会非常大。

因此本版本采用的是一个更务实的中间架构：

- 先做 `运行时 GUI 翻译服务`
- 通过统一入口处理大部分固定文案
- 后续如果要升级到 Qt 标准翻译体系，可以保留现有设置和启动链路，只替换翻译引擎

## 4. 整体架构

### 4.1 新增核心组件

新增文件：

- [GuiTranslator.h](/Users/wangzhao/dev/open Bloomberg/FinceptTerminal/fincept-qt/src/core/i18n/GuiTranslator.h)
- [GuiTranslator.cpp](/Users/wangzhao/dev/open Bloomberg/FinceptTerminal/fincept-qt/src/core/i18n/GuiTranslator.cpp)

该组件是 `v4.0.3` 中文化的核心。

### 4.2 组件职责

`GuiTranslator` 负责：

- 维护当前 GUI 语言状态
- 从设置中读取已保存语言
- 在运行时遍历顶层窗口和子控件
- 将固定英文 GUI 文案映射为简体中文
- 在切回英文时恢复原始文案

### 4.3 为什么要保存“原始英文”

如果直接把控件文字改成中文，后续再切回英文时，系统无法知道原始字符串是什么。

因此实现里对常见控件使用了“保存原始文案再翻译”的方式：

- 第一次处理控件时，先保存原文
- 切中文时，用原文查词典后替换显示
- 切英文时，直接恢复原文

这样可以避免：

- 多次切换后文案被污染
- 中文再被误当作源文案继续翻译

## 5. 翻译服务实现思路

### 5.1 翻译词典

当前翻译源采用内置词典：

- 英文固定 GUI 文案 -> 简体中文

词典主要覆盖：

- screen 标题
- Settings 导航
- 通用按钮
- 常见状态词
- News 详情面板中的固定 UI 文案
- 登录/注册等常见认证文案

### 5.2 运行时处理方式

`GuiTranslator` 通过以下方式工作：

1. 启动时初始化并安装到 `QApplication`。
2. 从 `SettingsRepository` 读取 `appearance.language`。
3. 根据当前语言，扫描所有顶层窗口。
4. 递归处理子控件。
5. 对识别到的 GUI 控件做文本替换。

### 5.3 当前覆盖的控件类型

当前实现主要覆盖：

- `QLabel`
- `QAbstractButton`
- `QGroupBox`
- `QLineEdit` 占位符
- `QComboBox` 条目
- `QListWidget` 条目
- `QTabBar` 标签
- `QMenu / QAction`
- `QWidget` 的 `windowTitle / toolTip / statusTip`
- `QTableWidget` 表头
- `QTreeWidget` 表头

### 5.4 文案格式兼容

翻译逻辑不是只做最简单的整串匹配，还额外兼容了几个常见格式：

- 首尾空白保留
- 标题中的 ` — ` 分隔
- 尾部 `:`
- 尾部 `...`

这样可以减少因为标题拼接方式不同而导致的翻译缺失。

## 6. 启动链路接入

相关入口文件：

- [main.cpp](/Users/wangzhao/dev/open Bloomberg/FinceptTerminal/fincept-qt/src/app/main.cpp)

接入方式：

- `QApplication` 创建后，初始化 `GuiTranslator`
- 数据库和设置迁移完成后，调用 `apply_saved_language()`

这样做的原因：

- 语言设置保存在数据库中，必须在数据库可用后读取
- 但翻译器本身要尽早装到应用对象上，才能拦截后续窗口显示

## 7. Settings 接入

相关文件：

- [SettingsScreen.h](/Users/wangzhao/dev/open Bloomberg/FinceptTerminal/fincept-qt/src/screens/settings/SettingsScreen.h)
- [SettingsScreen.cpp](/Users/wangzhao/dev/open Bloomberg/FinceptTerminal/fincept-qt/src/screens/settings/SettingsScreen.cpp)

本版在 `Appearance` 页新增了 `Display Language` 配置项：

- `English`
- `简体中文`

保存时做两件事：

1. 写入 `appearance.language`
2. 调用 `GuiTranslator::set_language_code(...)`

因此这版支持：

- 持久化
- 运行时切换

## 8. MainWindow 接入

相关文件：

- [MainWindow.cpp](/Users/wangzhao/dev/open Bloomberg/FinceptTerminal/fincept-qt/src/app/MainWindow.cpp)

主窗口接收 `language_changed` 信号后，会重新执行标题更新逻辑。

这样做是因为窗口标题不是纯静态文本，而是会随着当前 screen 变化，例如：

- `Fincept Terminal — Markets`
- `Fincept Terminal — News`

如果不重新计算标题，只做一次启动时替换，后续导航时标题会重新变回英文。

## 9. 版本更新点

本版还同步修改了若干版本显示点：

- `CMakeLists.txt` 项目版本改为 `4.0.3`
- 状态栏版本号改为 `v4.0.3`
- MCP client 上报版本改为 `4.0.3`
- `setup.sh` banner 改为 `v4.0.3`

## 10. 当前方案的优点

- 改造面小于全面 `tr()/QTranslator` 方案
- 可以快速覆盖大量现有硬编码 GUI
- 不要求一次性重构所有 screen
- 对默认英文用户几乎零影响
- 后续可以继续往词典里补充文案

## 11. 当前方案的局限

这套实现是 `v4.0.3` 的务实方案，不是终局方案，局限性很明确：

- 依赖词典覆盖率，不是所有页面都能自动命中
- 更适合固定 GUI 文案，不适合复杂动态语义内容
- 不是标准 Qt 国际化流水线
- 某些自绘控件、delegate、canvas、图表中的文本可能仍需单独处理

## 12. 后续扩展建议

如果后续要继续做中文化，建议按下面顺序推进：

1. 继续扩充 `GuiTranslator` 词典，优先补核心页面缺失项
2. 对命中率低的页面，逐步改造成集中式字符串定义
3. 新增页面时，避免继续散落硬编码字符串
4. 当 GUI 中文覆盖率足够高后，再评估是否切换到 Qt 标准 `.ts/.qm` 体系
5. 新闻自动翻译、AI 内容翻译应作为独立模块建设，不应混入 GUI 语言层

## 13. 维护建议

- 新增固定 GUI 文案时，优先补到 `GuiTranslator` 词典
- 如果某类控件显示仍是英文，先判断它是不是当前支持的控件类型
- 如果不是支持类型，优先在 `GuiTranslator` 中扩展控件处理逻辑，而不是到处手写替换代码
- 如果是业务内容而不是 GUI，不要放进这个翻译层

## 14. 总结

`v4.0.3` 的中文化架构本质上是：

- 一个轻量级运行时 GUI 翻译服务
- 一个持久化语言设置入口
- 一个尽量少改旧代码的兼容方案

它的目标不是一次性做完全部国际化，而是让产品先拥有一个真正可工作的中文 GUI 起点，并为后续持续演进留下清晰入口。
