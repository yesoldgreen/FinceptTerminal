# 《v4.0.3 gui中文化prd》

## 1. 文档信息

- 产品名称：`Fincept Terminal`
- 版本号：`v4.0.3`
- 文档类型：`PRD`
- 本版主题：`GUI 简体中文支持`
- 当前基线：`v4.0.2`

## 2. 背景

`Fincept Terminal v4.0.2` 已具备完整的终端功能，但整体交互以英文为主。对于中文用户，首次进入产品后的理解成本较高，尤其体现在导航、设置、按钮、状态提示、空状态、弹窗文案等 GUI 层面。

`v4.0.3` 不做内容翻译，不做新闻机器翻译，只聚焦 `GUI 中文化`，让中文用户可以把产品当作可直接使用的中文桌面终端。

## 3. 版本目标

- 支持界面语言在 `English` 与 `简体中文` 之间切换。
- 为主要 GUI 文案提供简体中文显示。
- 默认语言保持 `English`，不影响现有英文用户。
- 记住用户语言选择，下次启动自动恢复。
- 为后续更完整的多语言体系打下基础。

## 4. 本版本范围

### 4.1 包含

- `Settings` 中新增界面语言设置项。
- 应用支持 `English / 简体中文` 两种 GUI 语言。
- 主导航、常用页面标题、按钮、标签、状态提示、空状态、常见弹窗等界面文案支持中文显示。
- 语言切换后尽可能即时生效。
- 用户语言选择持久化保存。

### 4.2 不包含

- 新闻标题、新闻摘要、新闻正文翻译。
- AI 分析结果翻译。
- 第三方数据源返回内容翻译。
- 用户输入内容翻译。
- 帮助文档、官网内容、外链页面翻译。
- 繁体中文、日文、韩文等其他语言。

## 5. 用户问题

- 中文用户需要先适应英文 GUI 才能开始使用。
- 设置页和核心入口理解成本较高。
- 首次上手时，产品专业能力强，但语言门槛影响可用性和转化。

## 6. 目标用户

- 简体中文用户。
- 有金融、交易、研究、量化需求的中文用户。
- 首次接触 Fincept Terminal 的新用户。

## 7. 产品需求

### 7.1 语言切换

- 在 `Settings > Appearance` 中提供 `Display Language` 配置项。
- 可选项：
  - `English`
  - `简体中文`
- 默认值：`English`
- 语言设置保存后应持久化。
- 下次启动自动使用上次保存的语言。

### 7.2 生效方式

- 保存语言设置后，当前应用窗口应尽可能即时切换显示语言。
- 如果个别动态区域未能即时刷新，不应影响主流程使用。
- 不要求本版本实现完整的 Qt 官方 `.ts/.qm` 翻译工程。

### 7.3 中文化覆盖范围

优先覆盖：

- 主导航
- 主要 screen 标题
- Settings 页面
- 登录/注册/忘记密码相关页面的常见 GUI 文案
- News 界面中的固定 UI 文案
- 常见按钮与状态词
- 空状态和通用提示

## 8. 体验要求

- 中文文案应简洁、专业、偏终端风格。
- 产品品牌名 `Fincept Terminal` 可保留英文。
- 未收录翻译的文案应自动回退英文，不允许出现空白或异常占位。
- 不允许因语言切换导致页面不可用、崩溃或核心交互失效。

## 9. 技术策略要求

- 本版本允许采用 `运行时 GUI 文案映射` 方案，而不是一次性全面改造成 Qt 标准翻译文件体系。
- 应避免继续扩散新的硬编码文案路径。
- 需要建立后续可扩展的语言基础设施。

## 10. 验收标准

- 用户可在 `Settings` 中看到并修改 `Display Language`。
- 应用支持 `English` 与 `简体中文` 两种 GUI 显示。
- 保存后语言设置可被持久化。
- 重启应用后，语言设置能够恢复。
- 主要导航、设置、常见按钮和新闻详情固定 UI 文案可显示中文。
- 缺失翻译时可自动回退英文。
- 不出现主流程崩溃、白屏、按钮失效等回归问题。

## 11. 本版不做

- 新闻自动翻译
- AI API 翻译接入
- 中英双语并排显示
- 全量业务内容本地化
- 其他语言扩展

## 12. 发布说明建议

- `v4.0.3` 新增 GUI 简体中文支持。
- 可在 `Settings > Appearance > Display Language` 中切换界面语言。
- 本版本仅覆盖 GUI 中文化，不包含新闻与内容自动翻译。
